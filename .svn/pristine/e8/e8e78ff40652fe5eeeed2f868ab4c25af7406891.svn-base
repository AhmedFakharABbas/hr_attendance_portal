import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PortalBusinessLeaderComponent } from './portal-business-leader.component';

describe('PortalBusinessLeaderComponent', () => {
  let component: PortalBusinessLeaderComponent;
  let fixture: ComponentFixture<PortalBusinessLeaderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PortalBusinessLeaderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PortalBusinessLeaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
