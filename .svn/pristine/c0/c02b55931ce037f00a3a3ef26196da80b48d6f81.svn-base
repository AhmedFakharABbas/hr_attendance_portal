import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SlabDropdownComponent } from './slab-dropdown.component';

describe('SlabDropdownComponent', () => {
  let component: SlabDropdownComponent;
  let fixture: ComponentFixture<SlabDropdownComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SlabDropdownComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SlabDropdownComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
