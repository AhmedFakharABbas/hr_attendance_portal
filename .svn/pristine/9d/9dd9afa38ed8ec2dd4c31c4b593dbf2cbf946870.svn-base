import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SectionNewEmployeesComponent } from './section-new-employees.component';

describe('SectionNewEmployeesComponent', () => {
  let component: SectionNewEmployeesComponent;
  let fixture: ComponentFixture<SectionNewEmployeesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SectionNewEmployeesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SectionNewEmployeesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
