import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PortalsHomeComponent } from './portals-home.component';

describe('PortalsHomeComponent', () => {
  let component: PortalsHomeComponent;
  let fixture: ComponentFixture<PortalsHomeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PortalsHomeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PortalsHomeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
