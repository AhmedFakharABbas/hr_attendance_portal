import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AttendaceCorrectionComponent } from './correction/attendance-emp/attendance-emp.component';

describe('AttendaceCorrectionComponent', () => {
  let component: AttendaceCorrectionComponent;
  let fixture: ComponentFixture<AttendaceCorrectionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AttendaceCorrectionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AttendaceCorrectionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
