import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PortalEmployeeComponent } from './portal-employee.component';

describe('PortalEmployeeComponent', () => {
  let component: PortalEmployeeComponent;
  let fixture: ComponentFixture<PortalEmployeeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PortalEmployeeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PortalEmployeeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
