import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PortalLMComponent } from './portal-lm.component';

describe('PortalLMComponent', () => {
  let component: PortalLMComponent;
  let fixture: ComponentFixture<PortalLMComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PortalLMComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PortalLMComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
