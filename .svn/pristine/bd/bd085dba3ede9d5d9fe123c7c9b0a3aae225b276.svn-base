import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GrievanceTableComponent } from './grievance-table.component';

describe('GrievanceTableComponent', () => {
  let component: GrievanceTableComponent;
  let fixture: ComponentFixture<GrievanceTableComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GrievanceTableComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(GrievanceTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
