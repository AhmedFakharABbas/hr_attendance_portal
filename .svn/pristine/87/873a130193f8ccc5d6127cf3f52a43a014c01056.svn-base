import { ComponentFixture, TestBed } from '@angular/core/testing';

import { HRPopupComponent } from './hr-popup.component';

describe('HRPopupComponent', () => {
  let component: HRPopupComponent;
  let fixture: ComponentFixture<HRPopupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ HRPopupComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(HRPopupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
