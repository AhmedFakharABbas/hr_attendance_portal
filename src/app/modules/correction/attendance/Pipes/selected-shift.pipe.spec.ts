import { SelectedShiftPipe } from './selected-shift.pipe';

describe('SelectedShiftPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedShiftPipe();
    expect(pipe).toBeTruthy();
  });
});
